# Complete transliteration plan (SML bootstrap → sv0 compiler)

This document is a **roadmap** for moving the authoritative implementation from **`sv0c/sml/`** into **`.sv0`** sources under **`lib/`**, **`lexer/`**, **`parser/`**, and future compiler directories. It complements **`lib/LAYOUT.md`** (directory roles and dependency order) and **`task/sv0-toolchain-milestone-3-checklist.Rmd`** (phase table).

**Normative language rules** remain in **`sv0doc/`**. This file is **implementation planning** only.

## Keeping this plan current (**`PROGRESS.md` C-4**)

This file and **`lib/LAYOUT.md`** are the **paired** transliteration map for milestone 3. **`PROGRESS.md`** checklist **C-4** is satisfied when both stay aligned with the tree as seeds land.

**Maintainer contract**

1. When you add, rename, or drop a **`.sv0`** seed under **`lib/`**, **`lexer/`**, or **`parser/`**, update **this** §2 inventory row and **`lib/LAYOUT.md`** (directory table + transliteration §2) in the **same** integration when practical so readers never chase two divergent lists.
2. **Phase / row-level** tracking lives in **`task/sv0-toolchain-milestone-3-checklist.Rmd`** — link it from decomposition notes; do not fork a second full inventory here.
3. **Regenerate the snapshot** below after material **`bootstrap-sources.list`**, **`self-host-sv0-loop.list`**, stage0 goldens, or vm-parity manifest churn (counts are advisory; CI truth is the files).

## Lowering match environment (design — option **B**)

**Stakeholder choice (2026-04-23):** Thread match-time context beyond the item arena (**`item_tags` / `item_names` / `item_d2` / …**) using **(B) a documented packed `Vec<i32>`** (plus existing program-level vecs where they already exist), **not** **(A)** an ever-growing tail of parallel parameters on **`lower_expr_to_value`** and every tag helper.

**Rationale:** new fields (**per-fn param name/type pairs**, scrut-local stacks, future import-alias slices) should land as **new regions** in one **versioned** buffer with a single pack/unpack story. That maximizes **flexibility** and keeps **comprehensive** SML parity work from re-touching dozens of signatures (and from fighting bootstrap **function-count** / per-function **expression-depth** limits) every time the match pipeline gains another table.

**Sketch: `lower_match_env: Vec<i32>` v0 (indices are element offsets into the same vec)**

1. **`[0]`** — **`magic`**: sentinel **`0x4D450001`** (**`1297235969`** as **`i32`**) if the vec is a real env, or **`0`** when callers pass an **empty** vec and callees synthesize fresh callee tables locally (today’s **`lower_tag_match`** behaviour).
2. **`[1]`** — **`version`**: **`1`** for this layout.
3. **`[2]`** — **`fn_names_base`**: start index of the **`fn_names`** flat run (**`-1`** if none inlined).
4. **`[3]`** — **`fn_ret_base`**: parallel **`fn_ret`** run (**`-1`** if none).
5. **`[4]`** — **`fn_count`**: length of those two parallel runs ( **`0`** when tables are built fresh per match).
6. **`[5]`** — **`param_names_base`**: stride-1 name-token handles (**`-1`** until **`lower_fn`** threads params).
7. **`[6]`** — **`param_ty_base`**: stride-1 typedef / token handles (**`-1`** until wired).
8. **`[7]`** — **`param_count`**: **`0`** until wired.
9. **`[8]`** — **`scrut_base`**: start index of **interleaved** scrut-local rows **`(name_h, ty_h)`** (**`-1`** when empty).
10. **`[9]`** — reserved (**`-1`** in interleaved v0). **`lower_match_env_merge_parent_scrut`** also accepts legacy parallel parents (**`[8]`** = names base, **`[9]`** = types base).
11. **`[10]`** — **`scrut_count`**: scrut-local pair count (**`0`** until wired).

**Packing rule:** regions are **contiguous** sub-slices of **`lower_match_env`**; bases are **absolute indices**; lengths live in the header. Helpers **`lower_match_env_alloc`**, **`lower_match_env_slice`**, **`lower_match_env_set_fn_table`** (names) keep call sites small.

**Migration order:** (1) introduce vec + helpers + **`magic=0`** “legacy empty” path — **landed:** **`lower_match_env_*`** + **`test_lower_match_env_helpers`**; (2) move **`lower_top_fn_tables_for_match`** output into a packed region for **`lower_tag_match`** — **landed:** **`lower_match_env_append_top_fn_match_tables`**; (3) thread ItemFn param name / type-head token runs into **`[5..7]`** — **landed:** **`lower_match_env_append_item_fn_param_rows`** + **`lower_scan_item_fn_param_names_ty_toks`** (lowering-local token walk, no **`checker.sv0`**); **`match_scrut_cty`** reads callee tables from **`[2..4]`** and param tables from **`[5..7]`** directly (**no unpack** for probe); (4) scrut-local **`[8..10]`** — **landed (2026-04-23):** interleaved pairs via **`lower_match_env_append_scrut_pair`** / **`lower_match_env_merge_parent_scrut`** / **`lower_match_env_clone`**; **`match_scrut_cty`** probes packed scrut before params; **`lower_tag_match`** merges inherited env, records scrutinee 1-seg path after **`bind_scrut_var`**, threads **`match_env_inherit`** through **`lower_expr_to_value`**; **`lower_match_arms`** clones env + **`PatBind`** row per arm. Remaining: enum/tuple pattern rows that introduce scrut names (beyond **`PatBind`** / scrutinee path) if SML parity requires them.

**Session notes (non-normative)**

- **2026-04-22 (scrut merge/lookup unit test):** **`test_lower_match_env_scrut_merge_roundtrip`** covers **`lower_match_env_merge_parent_scrut`** for interleaved parents (**`append_scrut_pair`**) and parallel-layout parents (**`[8]`** names base, **`[9]`** types base), plus **`lower_match_env_scrut_ty_lookup`** (hit, **`limit`** shortfall, miss), legacy-empty parent merge no-op, and non-packed dst error (**`-1`**). Stage0 **`lowering.c`** + vm-parity **`lowering.sv0b`** refreshed.
- **2026-04-23 (later — migration (4) scrut `[8..10]`):** interleaved scrut tail + **`lower_match_env_merge_parent_scrut`** / **`lower_match_env_clone`**; **`match_scrut_cty`** **`lower_match_env_scrut_ty_lookup`**; **`lower_tag_match`** merge + scrutinee append + **`match_env_inherit`** threaded on **`lower_expr_to_value`** and helpers; **`lower_match_arms`** per-arm clone + **`PatBind`** **`append_scrut_pair`**; **`test_match_scrut_cty`** packed scrut case. Stage0 **`lowering.c`** + vm-parity goldens refreshed.
- **2026-04-23 (match env — migrations 1–3 + callee env read):** **(B)** packed **`lower_match_env`**. **(1)** header helpers + **`test_lower_match_env_helpers`**. **(2)** **`lower_tag_match`**: **`lower_match_env_append_top_fn_match_tables`**. **(3)** **`item_d3`/`item_d4`/`item_fn_row`** plumbed through lowering; **`lower_match_env_append_item_fn_param_rows`** fills **`[5..7]`** when **`item_fn_row >= 0`**; **`match_scrut_cty`** takes **`match_env`** and reads callee **`[2..4]`** + param **`[5..7]`** (unpack helper retained for **`test_lower_match_env_fn_pack_roundtrip`** only). Next: PatEnum / struct scrut-local rows if needed for full SML parity.
- **2026-04-22 (match context plumbing):** **`lower`** takes shared parser **`body_pp`** (path pool) and passes it to **`lower_fn`** (was empty **`Vec`**); **`lower_top_fn_tables_for_match`** fills **`match_scrut_cty`** callee **`fn_names`** from ItemFn rows with **`-1`** return-type placeholders; **`match_scrut_cty`** **`ExprLit` (0)** → **`-1`**; extended **`test_match_scrut_cty`**. Next: param / scrut-local **`Vec`**s + real per-fn return typedef handles from signatures.
- **2026-04-22 (later — `fn_ret` handles):** **`item_d2`** threaded through the lowering expression pipeline; **`lower_scan_fn_ret_ty_tok`** rescans from each ItemFn **name** token (fn generics + **`(`**…**`)`** skip with **`<>`** / **`[]`**, then **`->`**) and records the **first return-type token** for **`match_scrut_cty`** / **`expr_init_cty`** when **`has_ret`** (**`item_d2 % 2`**) is set; otherwise **`-1`**. Does **not** call **`parse_type`** (self-host TU order); exotic return shapes may still fall back to **`-1`**. Next: param / scrut-local **`Vec`**s; optional **`parse_type`** alignment once link order allows.
- **2026-04-22:** **`lower_match_arms`** **`PatEnum`** tuple ctor arms (`Enum::Var(pat,…)`) — parser **`ExprPatEnumMeta` (33)** + **`ExprPatFieldEmbed`** rows with payload slot index; lowering **`Assign`** from **`VMember(scrut, 0-10-pi)`** (SML **`VariantTuple`** **`bindPre`**).
- **2026-04-22 (later):** **`lower_tag_match`** calls **`match_scrut_cty`** on the scrutinee (empty fn/param/scrut-local **`Vec`**s until `lower_fn` threads real tables) so **`bind_scrut_var`** / **`PatBind`** can use **`DeclNamed`** for resolved enum/struct handles from **`pp`** / callee shape.
- **2026-04-22 (slice F):** **`parse_match_expr`** expands **`PatOr`** into multiple **`ExprMatchArm`** rows (shared guard/body) via **`pat_leftmost_subtree_rows`**; **`match_scrut_cty`** handles **`ExprStruct` (24)** like **`expr_init_cty`**; **`lower_match_arms`** **`PatLit`** negation for non-int/bool uses **`!=`** (**`Binop` 5**) instead of a stubbed-true condition.

**Snapshot (replace on churn)**

| Metric | Source | Count (this revision) |
|--------|--------|-------------------------|
| Bootstrap-listed programs | `lib/bootstrap-sources.list` (non-comment, non-empty lines) | 97 |
| Self-host pilots | `lib/self-host-sv0-loop.list` | 97 |
| Stage0 C golden files | `lib/golden/stage0/*.c` | 21 |
| VM parity allowlist | `test/vm-parity/manifest.txt` | 97 |

## Definition of done (milestone 3)

1. **Feature parity:** sv0 compiler matches SML on the agreed surface (see **`task/sv0-toolchain-milestone-3-self-host.Rmd`**).
2. **VM + C backends:** Both paths support the same corpus policies (VM parity manifest + allowlist; stage0 C goldens for bootstrap seeds).
3. **Self-host:** Native **`SV0_SELF_HOST_COMPILER`** can rebuild the compiler sources; **`bootstrap-sml-final`** tag and **`sml-legacy/`** layout when SML leaves the default path.

## Inventory: major `sml/` subtrees

| Area | SML path | sv0 today | Next focus |
|------|-----------|-----------|------------|
| Span / diagnostics | `sml/error/` | `lib/span.sv0` (flattened Pos/Span, **`span_merge`** (SML `merge`), **`pos_to_string`** (SML `posToString`), **`span_to_string`** (SML `toString`)), `lib/diagnostic.sv0` (severity, getLine, spaces/carets/padLeft, hasErrors, errorCount, **Vec-based diag list**, **sort-by-span**, **format_header**, **format_snippet**, **diag_int_to_str**, **format_location**, **format_diagnostic**, **`format_header_with_code`** (SML `codeStr` bracket), **`format_gutter`/`format_snippet_numbered`** (SML numbered gutter), **`format_related`/`format_help`** (SML related/help text), **`format_diagnostic_full`** (SML `format` — combined header+location+numbered snippet)) | `report`/`reportAll` I/O. **`span.sml` parity reached**; diagnostic.sv0 covers all SML `format` sub-expressions |
| Lexer | `sml/lexer/` | `lib/lexer.sv0` (**full tokenize pipeline**: char class, keywords, escape, scan string/char/number/op, `tokenize` → parallel Vec output, **`scan_escape`** (SML `scanEscape` — decode escape + advance pos, incl. `\xNN`)), `lexer/token.sv0` (98-variant enum) | **Pipeline parity reached** — remaining: integrate with parser token stream consumption |
| Parser | `sml/parser/` | `lib/parser.sv0` (binop/prec maps, token helpers, skip functions, **token stream nav** (peek/advance/expect/match), **parsePath**, **parseGenericParams** (extracts type param names from `<T, U: Bound>`), **skip helpers on token stream** (pub/unsafe/generics/where/attrs), 28 keyword constants, **`parse_use_item`** (SML `parseUseItem`), **`parse_module_decl`** (SML `parseModuleDecl`), **`is_assign_tok`** (SML `isAssignTok`), **`path_of_ty_valid`** (SML `pathOfTy` predicate), **`is_builtin_type_name`** (SML inline 18-primitive check), **`parse_type`** (SML `parseType` — full recursive-descent type parser with arena output: TyUnit/TyName/TyRef/TyRefMut/TyArray/TySlice/TyTuple + generic type args), **`path_pattern_from`** (SML `pathPatternFrom` — struct/enum pattern after path), **`parse_pat_atom`** (SML `parsePatAtom` — wildcard/bind/literal/tuple/paren/enum dispatch), **`parse_pat`** (SML `parsePat` — entry with `\|` for PatOr), **`parse_expr`** (SML `parseExpr` — full expression parser: precedence chain `parse_primary_expr`/`parse_postfix_expr`/`parse_unary_expr`/`parse_cast_expr`/`parse_mul_expr`…`parse_or_expr`/`parse_cmp_expr`/`parse_range_expr`, struct literal fields, `parse_arg_list`, `parse_struct_fields`, array body, tuples; 20 new functions)) **`parse_block`** (SML `parseBlock` — `{ stmt* [tail_expr] }`, stmt loop with `try_stmt`/`parse_expr`/`expr_ends_with_braced_block`), **`try_stmt`** (SML `tryStmt` — let/break/continue/assert/assign dispatch), **`parse_let_stmt`** (SML `parseLetStmt` — `let [mut] pat [: T] [= expr] ;`), **`try_assign_stmt`** (SML `tryAssignStmt` — `IDENT = expr ;` / `IDENT OP= expr ;`), **`parse_if_expr`** (SML `parseIfExpr` — recursive else-if), **`parse_while_expr`** (SML `parseWhileExpr` — with `loop_invariant(expr)` parsing, d3/d4), **`parse_for_expr`** (SML `parseForExpr`), **`parse_loop_expr`** (SML `parseLoopExpr`), **`parse_return_expr`** (SML `parseReturnExpr` — empty/value), **`expr_ends_with_braced_block`** (SML `exprEndsWithBracedBlock`); 10 new functions, stmt arena in expr arena (LetStmt=27, SemiStmt=28), control-flow dispatch in `parse_primary_expr`), **`parse_match_expr`** (SML `parseMatchExpr` + `parseMatchArms` — iterative arm loop with pattern/guard/body; **`ExprPatExtra=30`** holds PatLit/PatStruct/PatEnum row-0 payload before **`ExprMatchArm=29`**; arm **`ed2`** → extra index or legacy **`pat_d1`**), **`parse_quant`** (SML `parseQuant` — `forall`/`exists` as ExprCall), **`old`/`no_alias` dispatch** (ExprCall encoding in `parse_primary_expr`), **`parse_param`/`parse_param_list`** (SML `parseParam`/`parseParamList`), **`parse_contract`/`parse_contracts`** (SML `parseContract`/`parseContracts`), **`parse_fn_body`** (SML `parseFnBody` — ItemFn with generics, params, ret, contracts, body), **`parse_struct_item`** (SML `parseStructItem` — fields loop), **`parse_enum_item`** (SML `parseEnumItem` — unit/tuple/struct variants), **`parse_type_alias_item`** (SML `parseTypeAliasItem`), **`parse_trait_item`** (SML `parseTraitItem` — method loop), **`parse_impl_item`** (SML `parseImplItem` — optional trait for self, method loop), **`parse_item`** (SML `parseItem` — top-level dispatcher), **`parse_program`** (SML `val parse` — item loop until EOF). Item arena tags 0–7 with children-before-parents convention; **shared expression arena threading** (Batch 78): `parse_fn_body`/`parse_item`/`parse_trait_item`/`parse_impl_item`/`parse_program` accept and propagate `body_et`/`body_ed1-ed4`/`body_sf` so function body ASTs are preserved in a shared arena; `id4` repurposed for ItemFn `body_root` index, `contract_count` packed into `id2`) | **Parser transliteration complete** — all SML parser functions have sv0 equivalents; shared expression arenas unblock per-function body instruction fill for M3 lowering |
| Name resolution | `sml/name_resolution/` | `lib/env.sv0` (**SML parity**: scope mgmt, mod val/ty, fnArity, tyAlias, registerValueAlias, registerTypeAlias, lookupType, **`type_is_registered`** (combined prelude ∪ modTys — SML `typeIsRegistered`)), `lib/resolver.sv0` (intrinsics, path join, **path_join_vec** (Vec-based generalized join), **register_all_intrinsics** (env registration), **import alias mgmt** (add/lookup/count), **`variant_stem`** (SML `variantStem`), **`enum_variant_reg_path`/`enum_variant_reg_arity`** (SML `enumVariantReg`), **`pat_binds`** (SML `patBinds` — collect bound var handles), **`bind_pattern_locals`** (SML `bindPatternLocals`), **`register_item`/`register_items`** (SML `registerItem` — item-arena registration pass; enum variant registration deferred), **inline env lookups** (`tok_str`, `res_is_prelude_type`, `res_value_exists`, `res_type_exists` — string-based name comparison via token stream), **`resolve_pat_shape`** (SML `resolvePatShape` — pattern name validation with recursive arena walk), **`resolve_ty`** (SML `resolveTy` — type name validation; TyArray size expr deferred), **scope management** (`res_enter_scope`, `res_bind_local`, `res_lookup_in_frames_str`, `res_full_value_exists`, `res_lookup_fn_arity_str`), **`resolve_expr`** (SML `resolveExpr` — 27 expr tag cases with scope/binding/arity checking; ExprCast type validation deferred — type arena local to parser), **`resolve_stmt`** (SML `resolveStmt` — LetStmt/SemiStmt), **`resolve_contract`** (SML `resolveContract` — thin wrapper), **`apply_use_clause`/`apply_use_clauses`** (SML `applyUseClause` — use clause alias registration: value alias with arity propagation, type alias; enum variant aliases deferred), **`resolve_top_item`** (SML `resolveTopItem` — stub; item-body resolution deferred — needs parser enrichment), **`resolve_program`** (SML `resolve` — top-level orchestration: register_items → apply_use_clauses → resolve_top_items), helpers) | **Remaining:** `resolveFn`/`resolveStruct`/`resolveEnum`/`resolveTrait`/`resolveImpl` body traversals — **blocked on parser arena enrichment** (sub-tree data local to parser call sites) |
| Types | `sml/type_checker/types.sml` | `lib/types.sv0` (20-variant Ty enum, Box, Vec, ty_tag, fresh_var) | Mostly complete; string payloads pending |
| Unification | `sml/type_checker/unify.sml` | `lib/unify.sv0` (full structural unify over Ty, **`unify_list`** (pairwise Vec<i32> unification — SML `unifyList`; TyTuple/TyFn/TyNamed now element-wise)) | **Parity reached** (full unify + unifyList) |
| Type checker | `sml/type_checker/checker.sml` | `lib/checker.sv0` (binop class, type predicates, variant/ctor helpers, error codes, env helpers, **path_key_vec** (Vec-based n-segment join), **is_contract_param**, **struct/enum name extraction**, **`expect`** (type unification gate), **`fields_of_struct`** (struct-def index lookup), **`variant_shape_of`** (enum+variant search), **`assign_lhs_bad`** (error sentinel), **`enter_loop`/`exit_loop`** (SML `withLoop` decomposed), **`infer_lit`** (SML `synth` literal arms — literal tag → type tag), **`ctor_ty_arity`** (SML `ctorTy` param count), **`struct_field_ty`** (SML `tyOfStructField` via parallel type Vec), **`dup_variant_names`** (SML `dupVariants` — O(n^2) duplicate check), **`struct_enum_name_clash`** (SML `initTypes` clash check), **`named_only_ty`** (SML `namedOnlyTy` — struct/enum/none classifier), **`fields_of_struct_count`** (SML `fieldsOfStruct` field count by name handle), **`stmt_returns_arena`/`last_stmt_returns_arena`** (SML `stmtReturns`/`lastStmtReturns` arena variants), **`ast_ty_to_types_tag`** (SML `astTyToTy` primitive dispatch), **`type_param_push`/`type_param_restore`/`type_param_lookup`** (SML `withTypeParams` decomposed), **`resolve_field_ty_tag`** (SML `astTyToTy` + struct/enum/type param), **`contract_expr_new`/`enter`/`exit`/`active`** (SML `withContractExpr` decomposed), **`ty_import_alias_push`/`alias_lookup_bounded`** (SML `addTyImportAlias`/`clearTyImportAliases`), **`ctor_ty_tag`** (SML `ctorTy` type tag from shape), **`try_success_validate`** (SML `trySuccessPayloadTy` — ? operator validation), **`ast_ty_to_ty_payload`** (SML `astTyToTy` full dispatch + payload output), **`enum_ctor_count`** (variant count for enum), **`item_fn_ret_ty_tag`** (function return type), **`binop_result_ty_tag`/`unop_result_ty_tag`** (operator result type helpers), **`env_entry_index`** (SML `envEntry` index-returning lookup), **`fn_table_add`/`fn_table_lookup`/`fn_table_ret_type`/`fn_table_param_count`/`fn_table_param_type`** (function type table — parallel Vecs for function signatures needed by `modEnvFromProg` and `synth ExprCall`), **`scan_struct_field_names`** (token re-scan extracting struct field name positions — initTypes prerequisite), **`scan_enum_variant_names`** (token re-scan extracting enum variant name positions — initTypes prerequisite), **`type_param_lookup_str`** (string-based type param lookup — enables cross-position name comparison), **`scan_type_tag_at`** (token-stream type resolver — resolves type annotation at a token position to checker type tag; handles primitives, struct/enum, type params, Vec/Box, unit, tuple, ref, refmut), **`scan_struct_field_type_tags`** (struct field type tag extraction — combines scan_struct_field_names positions with scan_type_tag_at), **`scan_fn_param_type_tags`** (function parameter type tag extraction — walks fn signature tokens to extract each param's type tag; initTypes/modEnvFromProg prerequisite), **`fn_table_lookup_str`** (string-based fn table lookup for cross-scope resolution), **`scan_fn_ret_type_tag`** (token re-scan for function return type), **`item_fn_ty_to_table`** (SML `itemFnTy` — extract function type from item arena + register in fn table), **`register_all_item_fns`** (first part of `modEnvFromProg` — walk all ItemFn items, populate function type table), **`scan_enum_variant_shapes`** (token re-scan — classify variant as VSUnit/VSTuple/VSStruct from next-token), **`init_struct_defs`** (SML `initTypes` struct half — walk ItemStruct items, populate struct def table with field names+types via token re-scan), **`init_enum_defs`** (SML `initTypes` enum half — walk ItemEnum items, populate enum def table with variant names+shapes), **`struct_def_field_ty_str`** (string-based field type lookup in struct def table), **`enum_def_variant_shape_str`** (string-based variant shape lookup in enum def table); **bugfix**: `scan_enum_variant_names` now handles `(...)` tuple variant payloads (tag 6/7), **`builtin_fn_lookup`/`builtin_fn_ret_type`/`builtin_fn_param_count`/`builtin_fn_param_type`** (SML `modEnvFromProg` builtin registration — all 13 builtins: println, string_*, vec_*, box_*; uses integer IDs with TY_VAR for polymorphic positions), **`BUILTIN_COUNT`** (constant), **`resolve_fn_call`** (unified user fn + builtin lookup for synth ExprCall), **`edef_name_lookup_str`** (string-based enum def table name lookup — returns edef index or -1), **`ctor_type_from_edef`** (enum ctor type from def table — variant shape→TY_ENUM/TY_FN), **`resolve_ctor_path_ty`** (2-segment path → ctor type tag — synth ExprPath building block for enum constructors), **`sdef_name_lookup_str`** (string-based struct def table name lookup — mirrors edef_name_lookup_str), **`env_lookup_str`** (string-based env variable lookup — walks env_names backward comparing extracted strings; needed for cross-scope name resolution in synth), **`synth_call_ret_type`** (function call return type — resolves callee via fn_table/builtins and returns return type tag), **`synth_expr`** (SML `synth` expression dispatch — recursive type synthesis over expr arena with ret_ty+loop_depth+env_muts+ed4 threading; handles all 27 expression tags: ), **`synth_expr_call_simple`** (SML `synthExprCall` simplified — resolves 1-seg path callee via fn_table/builtins or 2-seg via ctor lookup, synthesises args, returns return type tag), **`check_stmt_in_block`** (SML `checkStmt` — processes LetStmt: synth init, extend env; SemiStmt: synth and discard), **`scan_fn_param_names`** (token re-scan for fn param name positions + mutability flags), **`check_fn_body`** (SML `checkFn` core — extend env with params, synth body, compare with return type), **`process_item_use`** (SML `oneUse` — 2-segment use path import: fn_table/sdef/edef lookup → env extension or type alias), **`build_mod_env`** (SML `modEnvFromProg` — module env builder: register all ItemFn as TY_FN in env, then process all ItemUse entries via `process_item_use`); **bugfix**: `infer_lit` now returns checker TY_* constants instead of types.sv0 enum ordinals) | Full `check` loop (per-fn body re-parse + `check_fn_body` — needs cross-module `parse_block`); **synth_expr complete** (all 27 tags), **check_fn_body complete**, **build_mod_env complete** |a`** (SML`stmtReturns`/`lastStmtReturns`— arena-aware, real expr arena tags 28/15), **`chk_tok_str`** (token text extraction), **`expr_references_result`** (SML`exprReferencesResult`— recursive expr arena walk for "result" path detection), **`ast_ty_to_types_tag`** (SML`astTyToTy` primitive dispatch — maps type name strings to `types.sv0`Ty tags: TyUnit=0, TyBool=1, TyChar=2, TyString=3, TyIsize=4, TyUsize=5, TyInt=7, TyUint=8, TyFloat=9; returns -1 for user/unknown), **`type_param_push`/`type_param_lookup`** (SML`withTypeParams`decomposed — push generic bindings into stride-2 Vec, lookup with logical-length restore), **`resolve_field_ty_tag`** (SML`resolveFieldTy` simplified — parser type arena node to Ty tag using struct/enum name tables + type param scope; `initTypes`building block), **`contract_expr_new`/`enter`/`exit`/`active`** (SML`withContractExpr`decomposed — scoped "in contract expr" flag via Vec tail), **`ty_import_alias_push`/`alias_lookup_bounded`** (SML`addTyImportAlias`/`clearTyImportAliases`— scoped push with logical-length restore), **`ctor_ty_tag`** (SML`ctorTy`— returns TY_FN for parameterised variants, TY_ENUM for nullary), **`try_success_validate`** (SML`trySuccessPayloadTy` — validates enum is valid `?` target: Result Ok/Err or Option Some/None with single-field tuple success variant; returns 1=Result, 2=Option, 0=not valid, -1=shape mismatch)) | **Full inference** (`synth`/`checkStmt`/`checkFn`/`initTypes`) — second largest deferred piece |
| Contracts | `sml/contract_analyzer/` | `lib/contract_analyzer.sv0` (identity pass-through) | **Parity reached** (analyzer is trivial in SML too) |
| Pipeline / driver | `sml/main.sml` | `lib/main.sv0` (paths, phase constants, CLI helpers, **`base_name_sv0`** (SML `baseNameSv0` — strip dir + `.sv0` ext)) | `readFile`/`compileFile`/`compileProjectDir` — I/O dependent |
| IR + lowering | `sml/ir/` | `lib/ir.sv0` (Value 8v/Expr 6v/Instr 17v enums, Box/Vec recursive), `lib/lowering.sv0` (QName split, C strings, **full 6-variant unop table**, **old_slot_name** (string concat), **param_name_tag**, **`param_name`** (SML `paramName` — extract handle from PatBind), **uniq_old_names** (Vec dedup), slot/enum/struct helpers, literal class, **`fresh_tmp_str`** (string `_sv0t<N>` — SML `freshTmp`), **`lower_lit_to_ir_tag`** (SML `lowerLit` dispatch), **`fn_ret_cty`** (SML `fnRetCty`), **import alias infra** (`lower_alias_new/add/lookup`), **`resolve_fn_callee`** (SML `resolveFnCallee`), **`resolve_enum_ctor_path`** (SML `resolveEnumCtorPath`), **`lower_field_index`** (SML local `fieldIndex` — struct field name→index), **`ast_ty_to_c_string_with_user`** (SML `astTyToCString` struct/enum typedef branch), **`find_fn_index`/`callee_fn_index`** (SML `calleeRetCty` decomposed — resolve alias then fn-table scan), **`scrut_local_lookup`/`param_ty_lookup`** (SML `matchScrutCty` helpers), **`handle_to_str`** (token handle→string), **`emit_struct_td`/`emit_enum_td`** (SML `emitStructTd`/`emitEnumTd` — C typedef generation), **`build_struct_order`** (SML `buildStructOrder`), **`build_enum_variants_data`** (SML `buildEnumVariants`), **`collect_typedefs_str`** (SML `collectTypedefs`), **`mentions_result`** (SML `mentionsResult` — expr arena walk for "result" path), **`all_old_names_expr`** (SML `allOldNames` — expr arena walk collecting `old(x)` handles), **`expr_init_cty`** (SML `exprInitCty` — infer type handle from init expr: ExprStruct/ExprPath(2-seg)/ExprCall/ExprTuple(1) dispatch, -1 for "int"), **`scan_lets`** (SML `scanLets` — walk ExprBlock for LetStmt with init, collect name+type handle pairs), **`try_variant_names_lookup`** (SML `tryVariantNames` — full arena lookup with string-based Ok/Err/Some/None detection; returns success+failure variant handles), **`find_variant_in_items`** (SML `findVariantAst` — find variant flat index within an enum's item arena), **`scrut_locals_push`/`scrut_local_lookup_bounded`** (SML `withScrutLocals` — push extra scrut bindings with logical-length restore), **`lower_alias_replace`** (SML `setImportAliases` — bulk alias table fill from parallel Vecs), **`match_scrut_cty`** (SML `matchScrutCty` — determine type handle for match scrutinee via scrut locals, fn params, **ExprStruct (24)** typename in `pp`, enum ctor path, or callee return), **`variant_shape_in_items`** (enum variant shape + field count lookup from item arena flat arrays), **local IR enum types** (Value/Expr/Instr matching codegen.sv0 layout + `ir_value_tag`/`ir_expr_tag`/`ir_instr_tag` accessors), **`ctr_fresh`** (mutable temp counter via Vec<i32> + vec_set), **`classify_assign_lhs_arena`** (SML `classifyAssignLhs` — flat arena ExprPath(1-seg)/ExprField(ExprPath,field) classification), **`lower_lit_to_ir_value`** (SML `lowerLit` → Value enum — lit tag + tok pos to IR Value), **`lower_expr_to_value`** (SML `lowerExprToValue` — core forms incl. **ExprStruct (tag 24)**: DeclNamed(struct typename tok: single-seg uses `pp[ed1]`; multi-seg `pp[ed1+ed2-1]` — last path component) + per-field StoreField using `lit_sf_names` + parser `ExprStruct` **`ed4`** as `sf_names` base index (**`test_lower_expr_struct_qualified_path`**); **ExprMethodCall (5)**: lower receiver + each explicit arg to **`Value`**, then **`Instr::Call`** with callee token **`ed2`** (method name position, same **`builtin_map`** index as path callees) and args **`[receiver, …]`** (mirrors `method(rcv,…)`; **`bid==14`** reads logical first arg at **`arg_vals[1]`**); **ExprArray (26)**: **`sv0_vec_new`** (**builtin 7**, **`Call`** **`fn_h=0-7`**) then **`sv0_vec_push`** (**8**, void **`Call`**) per element (children-before-parent indices **`idx - elem_count + k`**); **`VVar`** temp holds the vec handle; **ExprIndex (8)**: **`Instr::Call`** with **`fn_h = 0 - 10`** (**`sv0_vec_get`** / `CALL_BUILTIN` 10 — `vm_codegen` treats negative **`fn_h`** as builtin id **`0-fn_h`**) → **`VVar`** (non-`Vec` index forms deferred); **ExprRange (21)** runs `lower_for_effect` on optional lo/hi (no scalar range value); **ExprAssert (23)** uses `lower_for_effect` (runtime trap deferred): ExprLit/ExprPath(1-seg)/ExprUnop(+Borrow/BorrowMut→VAddrOf)/ExprBinop/ExprCall(1-seg general)/ExprField/**ExprTupleField (tag 7)** (tuple **VMember** with synthetic field handle **500+field_num**; **codegen.sv0** `name_of` → **fN**)/ExprBlock(inline LetStmt/SemiStmt)/ExprIf(with+without else)/ExprReturn/ExprAssign/ExprAssignOp/ExprTuple(1-elem)/ExprWhile(WhileLoop instr)/ExprFor(desugars to WhileLoop with ctr_fresh temps, Assign+increment pattern)/ExprLoop(WhileLoop(true,body))/ExprBreak(Break instr)/ExprContinue(Continue instr)/ExprCast(Unop(99,v))/ExprPath(2-seg enum ctor: DeclNamed+StoreField with -1 sentinel for "tag")/ExprCall(2-seg enum ctor: DeclNamed+StoreField tag + payload fields with -10..-1N sentinels)/ExprCall(1-seg builtin dispatch: builtin_map bounds-check + void/value/no_alias classification with negative sentinel callee IDs); enum metadata threading (enum_names/tag_offsets/tag_counts/tags_flat)/ExprTry(tag 22: ? operator — DeclNamed+Store+IfElse tag check with ReturnVal failure propagation and FieldAccess p0 success extraction; fn_ctx packed function type context for callee return type lookup); out-instrs accumulator pattern), **`value_to_expr`** (SML `valueToExpr` — VVar→Load, otherwise→Literal), **`lower_expr_with_instrs`** (SML `lowerExprWithInstrs` — wrapper: lower_expr_to_value + value_to_expr), **`lower_for_effect`** (SML `lowerExprForEffect` simplified — lower + discard value), **`lower_return`** (SML `lowerReturn` — lower expr + emit ReturnVal), **`lower_stmt`** (SML `lowerStmt` simplified — LetStmt: DeclVar+lower init+Store; SemiStmt: lower for effect), **`lower_block`** (SML `lowerBlock` — iterate stmts via lower_stmt + optional tail via lower_return), **`lower_body`** (SML `lowerBody` — ExprBlock→lower_block, otherwise→lower_return), **`lower_fn`** (SML `lowerFn` simplified, no contracts — creates fresh ctr, delegates to lower_body), **`lower_into_var_instrs`** (SML `lowerIntoVarInstrs` simplified — lower expr + Store(dest, expr)), **`bind_scrut_var`** (SML `bindScrutVar` — lower scrutinee to named temp; VVar passthrough, VInt/VBool→Assign, otherwise→DeclNamed+Store based on type handle), **`lower_match_arms`** (SML `lowerMatchArms` + inline `matchArmSetup` — reverse-iterate arms, build nested IfElse; PatWild/PatBind; **PatLit** via **ExprPatExtra** + `==` or `!=` scrut/literal (non-int/bool negation uses `!=`); **PatEnum** tag compare when `pp` path resolves; **PatStruct** tag compare when path has >=2 segments), **`lower_tag_match`** (ExprMatch tag 11 dispatch — bind scrutinee, alloc output temp, delegate to lower_match_arms; **`scrut_cty`** via **`match_scrut_cty`** with empty fn/param/scrut tables until orchestrator threads full context), **`lower`** (SML `Lowering.lower` — top-level program lowering orchestrator: builds struct/enum data, collects typedefs, extracts ItemFn label handles; body instruction lowering deferred — needs per-function expression arenas from parser)) | **Remaining:** **PatStruct** edge cases; thread **fn/param/scrut-local** tables into **`lower_tag_match`** so **`match_scrut_cty`** resolves 1-seg scrutinees (locals/params); per-function body instruction fill in `lower` (**wired** — Batch 79: `lower` calls `lower_fn` per ItemFn with shared body arenas, produces stride-4 blocks; params/return type use simplified defaults pending token re-scan); contract injection (`ensGlue`, `injectEnsuresAndRetSlot`, `lowerEnsuresClause`); **SML compiler function budget ~172 fn** (4 tests removed to date: test_fresh_tmp, test_split_qname, test_lower_into_var, test_bind_scrut_var; future batches may need file split) |
| Link | `sml/link/` | `lib/link.sv0` (is_sv0, mangling, strip, rewrite predicates, **fn_name_for_link** (main exemption), **link_dir_from_entry**, **backslash-aware find_last_slash**, **`map_path_segs_needs_mangle`/`map_path_segs_mangle_first`** (SML `mapPathSegs` — 1/2-segment path mangling), **`collect_top_names`** (SML `collectTops` — name handles into Vec), **`map_path_segs_rewrite_1`/`_2`** (SML `mapPathSegs` path rewriting), **`strip_link_directives`** (SML `stripLinkDirectives` — build filtered index Vec excluding ItemUse/ItemModule), **`split_module_name`/`split_module_body_start`** (SML `splitModule` — extract module name handle or use default)) | **`mapTy`/`mapExpr`/`mapItem`**, **`walkSv0`/`parseFile`/`linkProjectDir`** |
| C backend | `sml/backend/c/` | `lib/codegen.sv0` (**full** `emitValue`/`emitExpr`/`emitInstr`/`emitFn`/top `emit`, **is_main_block**, **emit_block_body**, **`c_escape_one` control-char parity** (ord < 32 → `\<ord>`), **`split_main_index`** (SML `splitMain` — main block index scan)) | **Parity reached** — all SML functions have named sv0 equivalents |
| VM backend | `sml/backend/vm/` | `lib/vm_codegen.sv0` (layout, opcodes, pool, builtins map, **fixed width_of_cty** (proper layout lookup), **enum field helpers**, **layout_lookup**, **`lookup_slot`/`slot_base`/`slot_width`** (SML `lookupSlot`), **`alloc_local`** (SML `allocLocal`/`bindParam`), **`replace_loop_exit_jump`** (SML `replaceLoopExitJump`), **`patch_continue_jumps`** (SML `patchContinueJumpsFrom` — continue-sentinel JUMP rewriting), **`insn_vec_stride`** (byte-to-element stride — **fixed** for bs=2: PUSH_BOOL/RETURN_SLOTS now stride=2), **`enum_field_names`** (SML `enumFieldNames`), **`value_width`** (SML `valueWidth`), **`bind_param`** (SML `bindParam` — slot_info with struct/enum width), **`struct_layouts_build`** (SML `structLayouts` — filter ItemStruct, collect name+field count), **`enum_layouts_build`** (SML `enumLayouts` — filter ItemEnum, width = 1+maxPayload), **`emit_value`** (SML `emitValue` — IR Value → bytecode; all scalar cases, VMember deferred), **`emit_expr`** (SML `emitExpr` — IR Expr → bytecode; Literal/Load/Binop/Unop with handle-based op resolution, FieldAccess deferred), **`emit_instr`** (SML `emitInstr` — IR Instr → bytecode; all 16 cases: Nop/DeclVar/DeclNamed/Assign/Store/StoreField/IfElse/WhileLoop/BlockInstr/Break/Continue/Call/Requires/Ensures/ReturnVoid/ReturnVal), **`emit_instrs`** (SML `emitBlock` — iterate Box<Instr> Vec), **`vec_append`**, **`emit_ret_width`**, **`flat_byte_size`** (walk flat insn Vec for jump offset calc), **`handle_to_str`** (token handle → string), **`index_of_fn`** (SML `indexOfFn` — fn name → fn_names table index), **`patch_break_jumps`** (scan for break sentinel, patch to end-of-loop), **`index_of_field_range`** (field lookup within flat per-slot fields), **`contract_pool_key`** (negative key encoding for contract messages), **`ensure_var_int`** (SML `ensureVarInt` — scalar slot if absent), **`scan_instr_env`/`scan_instrs_env`** (SML `scanInstr` — recursive env scan), **`local_count_and_env`** (SML `localCountAndEnv` — bind params + scan instrs, build env Vecs, return local count; field_starts placeholder until struct field storage), **`compute_arity`** (sum of param widths), **`emit_fn`** (SML `emitFn` — function-level orchestration: env + emit + arity + pool, appends stride-4 func entry), **`is_main_label`** (SML `isMain` — check if label = "main"), **`build_func_order`** (SML `funcOrder` — non-main first, main last), **`emit_program`** (SML `VmCodegen.emit` — top-level orchestration: build layouts, reorder blocks, emit all functions; returns pool + ft_entries for caller to encode via bytecode.sv0)), `lib/bytecode.sv0` (50 opcodes, sizes, LE helpers, file layout, **Vec-based LE encoding** (encode_u16_le/u32_le/i32_le), **encode_header**, **decode_u16_at/u32_at**, **verify_magic**, **`encode_strings`** (SML `encodeStrings` — string pool byte codec), **`encode_string_literals`** (handle-list variant), **`decode_strings`** (SML `decodeStrings` — reverse parse), **`encode_insn_at`** (SML `encodeInsn` — instruction-to-bytes encoding for all opcodes), **`decode_insn_at`** (SML `decodeInsnVec` — bytes-to-instruction decoding), **`encode_all_insns`** (SML `encodeAll`), **`decode_all_insns`** (SML `decodeAll`), **`encode_file`** (SML `encodeFile` — full .sv0b file assembly: header + string section + stride-4 func table + code section), **`decode_file`** (SML `decodeFile` — .sv0b file parsing with instruction roundtrip)) | **VM backend transliteration complete** — all SML `VmCodegen` functions have sv0 equivalents; for-loop continue optimization deferred |
| Include expansion | `sml/include_expand.sml` | `lib/include_expand.sv0` (trim, parse_include_line, path_ok, **split_lines**, **cycle detection**, **path_dir/path_join**) | **`expand`/`expandFile`** — file I/O dependent (all non-I/O building blocks done) |

## Suggested sequencing (high level)

1. **Parser + lexer closure** — one front-end entry that matches SML acceptance on a growing corpus.
2. **Resolver + checker** — same diagnostics categories as SML (`E0xxx` / `E05xx` where defined).
3. **IR lowering** — statement/expression coverage for bootstrap list + vm-parity manifest.
4. **Backends** — C first (existing goldens), then VM (bytecode goldens).
5. **Driver** — replace `Main.main` SML orchestration with sv0 `main` calling the same phases.

## Concrete workflow per slice

1. Pick a **vertical** (e.g. “`match` on structs”) or a **horizontal** file (e.g. `parser.sml` §N).
2. Add **`path.sv0`** under **`lib/`** or **`parser/`**, **`#include`** shared stubs as today.
3. Register in **`lib/bootstrap-sources.list`** if it must VM-compile standalone; add **`lib/golden/stage0/<stem>.c`** when C output is locked.
4. Optionally add **`lib/self-host-sv0-loop.list`** when the slice links as a single TU for **`cc`** smoke.
5. Run **`./scripts/sv0 test`**; refresh **`test/vm-parity/golden/sml`** only when manifest programs change.

## Related

- **`lib/LAYOUT.md`** — transliteration dependency order (§2).
- **`doc/self-host-sv0-loop.md`** — pilot loop and **`SV0_SELF_HOST_COMPILER`**.
- **`doc/compiler-passes.md`** — phase mapping for the current SML pipeline.
- **`doc/tooling-formatter-linter.md`** — **`sv0-formatter`** / **`sv0-linter`** design sketch (tooling; not spec).

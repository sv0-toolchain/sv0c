/* SS-U03d: returning a `let` binding that aliases a slice over a local —
   expects E0322 (native only). */
fn bad() -> &[i32] {
    let a: [i32; 4] = [1, 2, 3, 4];
    let s: &[i32] = &a[1..3];
    return s;
}
fn main() -> i32 { return 0; }

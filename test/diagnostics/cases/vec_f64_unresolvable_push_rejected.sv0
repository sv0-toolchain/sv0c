// E0447: the first argument is a call, not a declared name, so the element
// type of the vec is not visible and a float element is still refused.
fn make() -> Vec<i32> {
    let v: Vec<i32> = vec_new();
    return v;
}

fn main() -> i32 {
    vec_push(make(), 2.5);
    return 0;
}

// E0447: the generic vec_push stores an integer-sized word and would silently
// truncate an f64; the checker refuses it (use vec_push_f64) instead of
// compiling wrong code.
fn main() -> i32 {
    let v: Vec<i32> = vec_new();
    let x: f64 = 1.5;
    vec_push(v, x);
    return 0;
}

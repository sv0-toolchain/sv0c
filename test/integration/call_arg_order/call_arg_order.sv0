/* Regression: VM CALL must pass arguments left-to-right into formals (slot 0 = first param).
   1*100+2*10+3 = 123; any reversal would yield 321. */

fn pack3(a: i32, b: i32, c: i32) -> i32 {
  return a * 100 + b * 10 + c;
}

fn main() -> i32 {
  return pack3(1, 2, 3) - 123;
}

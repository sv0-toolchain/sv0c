struct Point { x: i32, y: i32 }

impl Point {
  fn sum(self: Point) -> i32 {
    return self.x + self.y;
  }
  fn plus(self: Point, k: i32) -> i32 {
    return self.sum() + k;
  }
}

fn main() -> i32 {
  let p: Point = Point { x: 8, y: 30 };
  return p.plus(4);
}

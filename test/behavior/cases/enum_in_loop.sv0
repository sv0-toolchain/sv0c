/* construct+match an enum each iteration — exits 10. */
enum Step { Go(i32), Stop }
fn main() -> i32 { let mut s: i32 = 0; let mut i: i32 = 0; while i < 5 { let st: Step = Step::Go(2); s = s + match st { Step::Go(n) => n, Step::Stop => 0 }; i = i + 1; } return s; }

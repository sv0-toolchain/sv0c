/* requires + loop_invariant satisfied — exits 10. */
fn f(n: i32) -> i32 requires(n >= 0) { let mut s: i32 = 0; let mut i: i32 = 0; while i < n { s = s + 1; i = i + 1; } return s; }
fn main() -> i32 { return f(10); }

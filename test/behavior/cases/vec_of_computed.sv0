/* push computed values — exits 20. */
fn main() -> i32 { let v: Vec<i32> = vec_new(); let mut i: i32 = 1; while i <= 4 { vec_push(v, i * 2); i = i + 1; } let mut s: i32 = 0; let mut j: i32 = 0; while j < vec_len(v) { s = s + vec_get(v, j); j = j + 1; } return s; }

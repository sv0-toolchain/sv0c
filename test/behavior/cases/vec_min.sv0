/* find min in a Vec — exits 1. */
fn main() -> i32 { let v: Vec<i32> = vec_new(); vec_push(v, 5); vec_push(v, 1); vec_push(v, 8); let mut m: i32 = vec_get(v, 0); let mut i: i32 = 1; while i < vec_len(v) { if vec_get(v, i) < m { m = vec_get(v, i); } i = i + 1; } return m; }

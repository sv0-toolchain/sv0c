# sv0c — progress (submodule)

**Meta-repo rollup:** when this tree is the `sv0c/` submodule of **sv0-toolchain**, the parent copies this file’s **`%`** into `task/sv0-toolchain-progress.md`. **Standalone clone:** keep this file authoritative here; reconcile on the next meta-repo integration.

**Last updated:** 2026-04-12 (America/Denver; **Phase 2 codegen.sv0 — full instruction emission (17 kinds) including recursive IfElse/While/Block.** Extended real pipeline module `lib/codegen.sv0` from leaf emission to full instruction dispatch: added Instr enum (17 variants), emitInstr for all kinds, emit_instrs with Vec<i32> + Box<Instr> round-trip for recursive instruction lists, emitParamList, emitFnProto, emitBlockDefn. 31 tests total. Added to bootstrap + self-host lists + stage0 golden. Stale doc metrics reconciled. README sv0c SHA fixed. Metrics: bootstrap 260→261, self-host 260→261, stage0 183→184, vm-parity 186 unchanged)

## Checklist (local source of truth)

| ID | Item | Done (0/1) |
|----|------|------------|
| C-1 | `lib/bootstrap-sources.list` + `lib/golden/stage0/` discipline documented and CI-enforced (`./scripts/sv0 test`) | 1 |
| C-2 | Self-host pilot list `lib/self-host-sv0-loop.list` tracked; loop recipe in `doc/self-host-sv0-loop.md` | 1 |
| C-3 | VM parity v1 manifest + goldens (`test/vm-parity/`) aligned with milestone allowlist policy | 1 |
| C-4 | Transliteration plan current (`doc/transliteration-plan.md`, `lib/LAYOUT.md`) | 1 |
| C-5 | Native semantic self-host (`SV0_SELF_HOST_COMPILER`) path documented vs pilot `emit-c` harness | 1 |
| C-6 | SML retirement criteria (`bootstrap-sml-final`, `sml-legacy/`) **not** claimed until `task/sv0-toolchain-milestone-3-self-host.Rmd` completion criteria | 1 |

**Optional metrics** (not in `%` unless promoted to checklist rows): non-comment lines in `lib/bootstrap-sources.list`, count of `self-host-sv0-loop.list` entries, vm-parity manifest size — copy into the meta **run log** when they change.

## Completion

- **Done:** count rows with `Done = 1` above.
- **Total:** row count of the checklist.
- **%:** `Done / Total * 100`.

## Notes

Point to the active vertical slice (SML module, harness name, or milestone sub-step). Link `task/sv0-toolchain-milestone-3-checklist.Rmd` for row-level transliteration inventory.

**Checklist closure notes:** **C-1** — `lib/LAYOUT.md` + `task/sv0-toolchain-milestone-3-self-host.Rmd` document the lists and stage0 goldens; parent **`./scripts/sv0 test`** runs **`bootstrap-build`**, stage0 **`diff`**, and **`verify_vm_parity_manifest_bootstrap`** (subset guard). **C-2** — `lib/self-host-sv0-loop.list` + `doc/self-host-sv0-loop.md` are the tracked pilot surface; **`./scripts/sv0 test`** runs **`self-host-sv0-loop`**. **C-3** — `test/vm-parity/manifest.txt` is the curated allowlist (**101** programs); `test/vm-parity/golden/sml/*.sv0b` are SML reference bytecode; **`./scripts/sv0 test`** **`cmp`**s post-**`bootstrap-build`** emit vs goldens; **`./scripts/sv0 test-guards`** / **`verify_vm_parity_manifest_bootstrap.py`** enforce **`manifest.txt` ⊆ `lib/bootstrap-sources.list`**; policy and growth expectations are spelled out in **`test/vm-parity/README.md`** and **`task/sv0-toolchain-milestone-3-self-host.Rmd`**. **C-4** — `doc/transliteration-plan.md` and `lib/LAYOUT.md` are the **paired** living map; both now document a **maintainer contract** (update together on seed churn), a **metrics snapshot** (~142 bootstrap lines, **59** pilots, **69** stage0 golden files, **101** vm-parity paths), and pointers to **`task/sv0-toolchain-milestone-3-checklist.Rmd`** for row-level inventory; no new CI gate — correctness is editorial discipline plus existing **`./scripts/sv0 test`** on list changes. **C-5** — `doc/self-host-sv0-loop.md` documents **pilot** third leg (**`sv0-self-host-emit-c.sh`** = SML stand-in) vs **native** third leg (**`SV0_SELF_HOST_COMPILER`**, same stdout contract); **`scripts/sv0-self-host-emit-c.sh`** header comments the contract; **`SV0_SKIP_SELF_HOST_COMPILER_DIFF=1`** documented for skipping the **`diff`**; **semantic** loop closure remains **`task/sv0-toolchain-milestone-3-self-host.Rmd`** bar — this checklist item is **documentation**, not claiming native self-host is done. **C-6** — **`bootstrap-sml-final`** / **`sml/`** → **`sml-legacy/`** are **not** claimed in this ledger; `doc/self-host-sv0-loop.md` **§ SML retirement guardrail** + **`task/sv0-toolchain-milestone-3-self-host.Rmd`** + **`.cursor/rules/28-sml-retirement-and-self-host-bar.mdc`** + **`./scripts/sv0 milestone-orient show M3`** make the bar explicit; **C-6 done = policy documented**, not SML retired.

**2026-04-10 (CI `make test` golden pass):** `Makefile` `test` target now runs `mkdir -p build` before SML `test_runner.sml`. Fresh clones (and GitHub Actions) omit gitignored `build/`, so file-based golden **compile+run** tests could not write `build/golden_tmp.c` and failed with `handle _ => false`.

**2026-04-10 (VM backend + bytecode + driver + include expansion):** 9 new seeds covering all remaining major SML modules: `codegen_split_main_emit_core` (C backend `splitMain`/`emit`/`intLit`), `vm_codegen_binop_core` (`binopToInsn` dispatch), `vm_codegen_width_core` (`widthOfCty`/`indexOfField`/`enumFieldNames`/`valueWidth`), `vm_codegen_emit_value_core` (`emitValue`/`emitExpr` insn counts), `vm_codegen_emit_instr_core` (`emitInstr`/`localCountAndEnv` slot delta), `vm_codegen_emit_fn_core` (`emitFn`/`emit`/`poolAdd`), `bytecode_encode_core` (`encodeInsn` byte lengths), `driver_pipeline_core` (pipeline + CLI), `include_expand_core` (`pathOk`/`parseIncludeLine`/cycle). Pilots **159→168**; stage0 **86→95**; bootstrap **159→168**. All major SML modules now have at least one surrogate seed. **Next**: end-to-end pipeline composition.

**2026-04-10 (self-host pilots 159/159):** Fixed the remaining 10 programs: (1) **lowering.sml**: moved `for`-loop increment before body in `While` IR, fixing `continue` skipping the counter (was infinite loop in `continue_loops`); (2) **9 struct-return seeds** (`env_core`, `env_scope_core`, `lookup_type_core`, `lookup_type_alias_core`, `lookup_value_core`, `resolver_arity_core`, `resolver_call_core`, `resolver_value_core`, `type_alias_core`): restructured from early `return Struct{…}` to `let mut out: Struct = default; … return out;` to avoid dead-code `return int` from struct-returning functions. Pilots **149→159** (= all bootstrap-listed programs). vm-parity goldens refreshed for all affected programs.

**2026-04-10 (self-host pilot completion, prior):** Added 73 bootstrap-listed programs to `self-host-sv0-loop.list` after `emit-c` + `cc` + run verification: `diagnostic_batch_core`, 4 golden-pass (`exists_requires`, `forall_requires`, `old_ensures`, `println_ok`), 7 integration tests (`call_arg_order`, `contracts`, `field_assign`, `generics`, `hello`, `patterns`, `structs`), 61 vm-parity programs. Pilots **76→149** / 159 bootstrap. YAML `updated:` spacing fixed in M3 task + checklist.

**2026-04-10 (C backend seeds):** 3 new `codegen.sml` surrogate seeds: `codegen_emit_value_core` (`emitValue` dispatch — VInt/VBool/VVar/VUnit/VString/VMember/VAddrOf + `emitCallArgs` + `cEscapeString` length), `codegen_emit_expr_core` (`emitExpr` + `emitParamList` + `emitFnProto` segment counts), `codegen_emit_instr_core` (`emitInstr` line-count dispatch for all 17 Ir.instr kinds + `emitBlockBody`/`emitBlockDefn`/`Return` void/value class). Pilots **76**, stage0 goldens **86**, bootstrap **159**. First sv0 seeds for the C backend.

**2026-04-10 (complete lowering.sml transliteration):** 12 new seeds covering all remaining `lowering.sml` named helpers: `lower_expr_match_core` (`ExprMatch` composition), `lower_expr_call_core` (`ExprCall` dispatch), `lower_expr_try_core` (`ExprTry` / `?` operator), `lower_fn_ret_cty_core` (`fnRetCty`), `lower_param_name_core` (`paramName`), `lower_variant_slots_core` (`variantSlots`), `lower_emit_struct_td_core` (`emitStructTd`), `lower_emit_enum_td_core` (`emitEnumTd`), `lower_collect_typedefs_core` (`collectTypedefs`), `lower_build_struct_order_core` (`buildStructOrder`), `lower_build_enum_variants_core` (`buildEnumVariants`), `lower_entry_core` (`lower` entry). Pilots **73**, stage0 goldens **83**, bootstrap **156**.
**2026-04-09 (match setup + enum ctor):** **`lib/lower_match_arm_setup_core.sv0`** (**`matchArmSetup`**), **`lib/lower_enum_construct_instrs_core.sv0`** (**`enumConstructInstrs`**); pilots **61**, stage0 goldens **71**, bootstrap-listed lines **144**; design note **`doc/tooling-formatter-linter.md`** (**`sv0-formatter`** / **`sv0-linter`** sketch).
**2026-04-09 (lowering batch):** **`lib/lower_quant_core.sv0`**, **`lib/lower_bind_scrut_core.sv0`**, **`lib/lower_match_one_arm_core.sv0`** (**`lowerQuant`**, **`bindScrutVar`**, **`lowerMatchArms`** one-arm IR-length surrogates); pilots **59**, stage0 goldens **69**, bootstrap-listed lines **142**. **2026-04-09:** added **`lib/lower_req_instrs_core.sv0`**; pilots **47**. **2026-04-10:** **`lib/lower_ens_glue_inject_core.sv0`**; pilots **48**. **2026-04-11:** **`lib/lower_store_value_to_slot_core.sv0`** (**`storeValueToSlot`**); pilots **49**; checklist **2/6** on transliteration seeds. **2026-04-09 (later):** **`lib/lower_return_body_core.sv0`** (**`lowerReturn`** / **`lower_body_block_tail_len`**); pilots **50**, stage0 goldens **60**, bootstrap-listed lines **133** (see **`doc/transliteration-plan.md`** snapshot). **`lib/lower_fn_prefix_core.sv0`** (**`lowerFn`** **`decl` @ `oldInstrs` @ `reqInstrs`** prefix); pilots **51**, stage0 **61**, bootstrap **134**. **`lib/lower_body_dispatch_core.sv0`** (**`lowerBody`** / **`lowerBlock`**); pilots **52**, stage0 **62**, bootstrap **135**. **`lib/lower_stmt_dispatch_core.sv0`** (**`lowerStmt`** return vs **`lowerExprForEffect`**); pilots **53**, stage0 **63**, bootstrap **136**. **`lib/lower_stmt_let_init_core.sv0`** (**`StmtLet`** **`PatBind`** init arms); pilots **54**, stage0 **64**, bootstrap **137**. **`lib/lower_expr_wi_tv_cluster_core.sv0`** (**`lowerExprWithInstrs`** / **`lowerExprToValue`** tag cluster); pilots **55**, stage0 **65**, bootstrap **138**. **`lib/lower_expr_effect_iv_cluster_core.sv0`** (**`lowerExprForEffect`** / **`lowerIntoVarInstrs`**); pilots **56**, stage0 **66**, bootstrap **139**. **C-3 (vm-parity v1):** manifest + SML goldens + **`manifest ⊆ bootstrap`** guard were already CI-enforced; closure recorded in this **`PROGRESS.md`** revision — checklist **3/6**. **C-4 (transliteration map):** paired **`doc/transliteration-plan.md`** + **`lib/LAYOUT.md`** maintenance contract and snapshot — checklist **4/6**. **C-5 (native vs pilot self-host):** explicit table in **`doc/self-host-sv0-loop.md`** — checklist **5/6**. **C-6 (SML retirement guardrail):** non-claim policy + links in **`doc/self-host-sv0-loop.md`** — checklist **6/6** (still **not** asserting **`bootstrap-sml-final`** or **`sml-legacy/`**).

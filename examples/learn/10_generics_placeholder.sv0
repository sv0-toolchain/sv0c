/* Parametric generics (<T>) are still evolving; this mirrors test/integration/generics/. */

fn id_i32(x: i32) -> i32 {
  return x;
}

fn main() -> i32 {
  return if id_i32(99) == 99 { 0 } else { 1 };
}
